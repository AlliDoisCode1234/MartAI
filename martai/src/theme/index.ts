import { defaultSystem } from '@chakra-ui/react';

// Custom theme configuration
// Extend this with custom tokens, colors, fonts, etc. as needed
export const theme = defaultSystem;

// Example: To customize, you can use createSystem and defineConfig:
// import { createSystem, defineConfig } from '@chakra-ui/react';
// export const theme = createSystem(defaultSystem, defineConfig({
//   theme: {
//     tokens: {
//       colors: { ... },
//       fonts: { ... },
//     },
//   },
// }));

